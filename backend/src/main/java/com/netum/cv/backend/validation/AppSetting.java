package com.netum.cv.backend.validation;

public enum AppSetting {

    // add settings values like username length
    // make mthods that recognize the value and change it to valid
    // primitive datatype by adding some codes on start or end of value;
}
